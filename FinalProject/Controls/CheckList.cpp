#include "CheckList.h"
#include <iostream>

CheckList::CheckList(short left, short top, short width, Border* border, Color textColor, Color backgroundColor, vector<string> entries)
{
    for (int i=0; i<entries.size(); i++)
    {

    }
}